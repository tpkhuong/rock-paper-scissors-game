import React from "react";
import ReactDOM from "react-dom/client";
import App from "./Components/App/app";

const rootElement = ReactDOM.createRoot(document.getElementById("app-wrapper"));

rootElement.render(<App />);
